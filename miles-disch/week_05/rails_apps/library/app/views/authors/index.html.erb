<h1>Authors index coming soon!</h1>

<% @authors.each do |author| %>
	<p><%= link_to( author.name , author ) %></p>
	<%= link_to(image_tag(author.image, :class => 'thumb' ), author) %>
<% end  %>

<%# "ctrl shift ." for awesomeness you need to be in a erb file (anything in the views folder ) -%> 
